version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:lts
    working_directory: ~/VocabulaRBy
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: npm install .
      - run:
          name: Test
          command: npm test
